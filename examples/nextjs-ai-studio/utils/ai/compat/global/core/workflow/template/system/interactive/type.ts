export type ToolCallChildrenInteractive = {
  params: {
    toolParams: {
      memoryRequestMessages: any[];
      toolCallId: string;
    };
  };
};

export type WorkflowInteractiveResponseType = any;
